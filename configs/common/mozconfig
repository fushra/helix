
ac_add_options --with-app-basename="Helix"
ac_add_options --with-app-name=helix
export MOZ_APP_DISPLAYNAME="Helix"

# Options for client.mk.
# mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/../newapp-sym

# Enables sccache to speed up builds
ac_add_options --with-ccache=sccache

# Options for 'configure' (same as command-line options).
ac_add_options --enable-tests
ac_add_options --disable-debug
ac_add_options --enable-optimize
ac_add_options --enable-debug-symbols
ac_add_options --disable-install-strip

ac_add_options --enable-project=helix
ac_add_options --with-app-basename=helix

# Enables sccache to speed up builds
ac_add_options --with-ccache=sccache

# Options for 'configure' (same as command-line options).
ac_add_options --enable-tests
ac_add_options --enable-debug
ac_add_options --enable-bootstrap